---
import { Layout as LayoutReact } from './Layout';
import SEO from '../seo/SEO.astro';
import type { SEOProps } from '@/types';

interface Props {
  seo?: SEOProps;
}

const { seo } = Astro.props;
---

<>
  <SEO {...seo} />
  <LayoutReact client:load>
    <slot />
  </LayoutReact>
</> 