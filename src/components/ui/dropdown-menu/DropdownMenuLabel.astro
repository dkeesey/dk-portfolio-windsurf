---
import { DropdownMenuLabel as DropdownMenuLabelPrimitive } from '@/components/ui/dropdown-menu'

interface Props {
  inset?: boolean
}

const { inset } = Astro.props
---

<DropdownMenuLabelPrimitive 
  client:load 
  inset={inset}
>
  <slot />
</DropdownMenuLabelPrimitive> 